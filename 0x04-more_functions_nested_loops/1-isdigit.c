/**
 * _isdigit - checks for a digit between 0 and 9
 * @c: input variable
 *
 * Return: 1 if c is a digit otherwise 0
 */
int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
		return (1);
	else
		return (0);
	return (0);
}
