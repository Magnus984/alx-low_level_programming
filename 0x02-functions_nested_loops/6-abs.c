#include "main.h"

/**
 * _abs - computes the absolute value of  a number
 * @n: parameter input
 *
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	else if (n == 0)
		return (n);
	else
		return (n);
	return (0);
}
